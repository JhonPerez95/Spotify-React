import { makeStyles } from '@material-ui/core/styles';

// Material UI
export const useStyles = makeStyles({
  root: {
    maxWidth: 345,
    minWidth: 290,
    margin: '1em',
    textAlign: 'center',
    margin: '2em',
    padding: '1em',
  },
  media: {
    minHeight: 315,
  },
});
